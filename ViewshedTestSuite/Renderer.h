#pragma once
#include "Camera.h"
#include "KeyboardHandler.h"

// Class used to do actual rendering
class Renderer
{
public:
	Renderer();
	~Renderer() = default;

private:
	//Camera camera; // Needed for getting camera matrices and the likes
	//KeyboardHandler keyHandler; // In order to handle keypresses


};

